#!/bin/sh

SCRIPT_PATH="$(cd "$(dirname "$0")/.." && pwd)/scripts/precommit-gate.ps1"
if command -v cygpath >/dev/null 2>&1; then
  SCRIPT_PATH="$(cygpath -w "$SCRIPT_PATH")"
fi

pwsh -NoLogo -NoProfile -ExecutionPolicy Bypass -File "$SCRIPT_PATH"
exit $?

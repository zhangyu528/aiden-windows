# 产品需求-FRD

## 1. 文档定位

本文档定义 Aiden Windows 本地托盘监控产品的目标用户、业务范围、核心需求与验收边界。

## 2. 产品目标

- 在 Windows 提供低打扰托盘监控体验。
- 面向 Gemini/Codex/Claude Code CLI 用户展示核心 telemetry 消耗信息。
- 在 UI 关闭后仍保持后台采集连续性。

## 3. 用户与场景

目标用户：
- 个人开发者
- 小团队研发用户

核心场景：
- 使用 CLI 时查看实时 token/cost/context 状态。
- 临时关闭托盘 UI 后，后台采集持续。
- 需要快速判断当前链路是否在线（Collector/VM/Agent）。

## 4. 范围

In Scope：
- 托盘主面板与多 CLI 页签（Gemini/Codex/Claude）
- 关键指标展示（Input/Output/User/Active/Cost/Context/Status）
- 手动刷新与自动刷新
- 首次引导与 CLI 开通配置管理
- 后台守护与重启能力（RuntimeAgent）

Out of Scope：
- 长期趋势分析与报表系统
- 终端日志全文检索展示
- Web 管理控制台
- 无缝热升级（允许短时中断）

## 5. 核心需求

### 5.1 托盘与主面板
- 用户可通过托盘图标打开/关闭面板。
- 主面板显示核心指标字段，支持手动刷新。
- 自动刷新按配置周期执行。

### 5.2 多 CLI 页签
- 提供 Gemini/Codex/Claude 三个页签。
- 页签可用前提：对应 CLI 已安装且已开通。
- 不可用页签禁用不可点击。
- 当前页签失效时自动回退到可用页签。

### 5.3 指标显示规则
- 当前用户未知时，用户相关字段显示 `Unknown` 或 `N/A`。
- 采集链路异常时，状态显示 `Offline`。

### 5.4 后台连续采集
- UI 退出后后台采集持续运行。
- Agent 负责守护 VM 与 Collector，支持健康检查与重启。

## 6. 非功能与体验约束

- 低打扰（托盘形态、轻量交互）
- 可恢复（异常后可自动恢复采集）
- 可观测（用户可见当前在线状态）

## 7. 验收口径（产品级）

- 主面板可稳定展示核心字段与状态。
- 页签可用性与回退行为符合规则。
- UI 关闭后采集不中断。
- 常见依赖故障下，状态可正确反映在线/离线。

## 8. 相关文档

- 功能规格：`docs/功能规格-FSD.md`
- 技术设计：`docs/技术设计-TDD.md`
- 自动化测试：`docs/自动化测试-ATD.md`
